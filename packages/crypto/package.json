{
  "name": "@heat/crypto",
  "version": "0.1.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/heat-js/monorepo.git"
  },
  "bugs": {
    "url": "https://github.com/heat-js/monorepo/issues"
  },
  "scripts": {
    "test": "yarn run -T jest",
    "patch": "if yarn test; then yarn version --patch; yarn run -T ncp; fi",
    "minor": "if yarn test; then yarn version --minor; yarn run -T ncp; fi",
    "major": "if yarn test; then yarn version --major; yarn run -T ncp; fi",
    "deploy": "if yarn test; then yarn version; yarn run -T ncp; fi"
  },
  "jest": {
    "preset": "jest-preset-coffeescript",
    "testEnvironment": "node",
    "rootDir": "./test/"
  },
  "devDependencies": {
    "bcrypt": "^3.0.3"
  }
}
