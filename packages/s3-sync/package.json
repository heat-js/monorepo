{
	"name": "@heat/s3-sync",
	"version": "0.0.13",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/heat-js/monorepo.git"
	},
	"bugs": {
		"url": "https://github.com/heat-js/monorepo/issues"
	},
	"scripts": {
		"patch": "yarn version --patch; yarn run -T ncp",
		"minor": "yarn version --minor; yarn run -T ncp",
		"major": "yarn version --major; yarn run -T ncp",
		"deploy": "yarn version; yarn run -T ncp"
	},
	"babel": {
		"plugins": [
			"@babel/plugin-transform-modules-commonjs"
		]
	},
	"dependencies": {
		"array-chunk": "^1.0.0",
		"async": "^3.2.0",
		"aws-sdk": "^2.639.0",
		"content-security-policy-builder": "^2.1.0",
		"etag-hash": "^1.0.1",
		"mime-types": "^2.1.26",
		"recursive-readdir": "^2.2.2"
	}
}
